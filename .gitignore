# ── Secrets — 절대 커밋 금지 (Private repo 라도 마찬가지) ──────────
.env
.env.local
.env.*.local
*.key
*.pem
# .env 가 포함된 모든 압축본 (실수 방지)
env.zip
env*.zip
.env.zip

# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
env/
.ipynb_checkpoints/
*.egg-info/
dist/
build/

# ── IDE / OS ──────────────────────────────────────────────────────
.idea/
.vscode/
.DS_Store
Thumbs.db
desktop.ini

# ── LangSmith / 기타 ──────────────────────────────────────────────
.langsmith/

# ── Claude Code 로컬 설정 (개인 환경) ─────────────────────────────
.claude/

# ── 임시 작업 파일 ────────────────────────────────────────────────
*.log
*.tmp
*.bak
_devguide.txt

# ── 발표 자료용 가이드 (개인 작업, 커밋 제외) ──────────────────────
docs/PROGRESS_II_GUIDE.md

# ── docx 일회성 편집 스크립트 (개인 작업, 커밋 제외) ────────────────
_edit_devguide.py
_fix_devguide_styles.py

# ── 협업 정책 (Private repo) ──────────────────────────────────────
# 본 저장소는 팀 내부 협업용 Private repo 입니다.
# 실험 산출물 (results/, AI 시드) 은 *포함*하여 팀원 간 공유합니다.
# 향후 Public 전환 시 docs/06 + docs/07 의 Redaction 정책 적용 필요.
